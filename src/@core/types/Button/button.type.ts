// The available button colors
export type ButtonColor = 'blue' | 'green' | 'red' | 'yellow' | 'gray';

// The available HTML button types (defines the content style)
export type ButtonHtmlType = 'button' | 'submit' | 'reset';

// The available button variants (defines the content display on the button)
export type ButtonVariant = 'icon' | 'text' | 'iconText';

// The available visual types of the button (defines the button shape)
export type ButtonType = 'rounded' | 'default';